# Copy to .env and fill in. Do not commit .env.

# Neon などの Postgres 接続文字列
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# 本番では必ず設定。空だと誰でも投稿できる
APP_PASSWORD=

# Slack Incoming Webhook URL（サーバーのみ）
SLACK_WEBHOOK_URL=
